version: '3.8'

services:
  wsgi:
    container_name: django_wsgi
    build:
      context: ../..
      dockerfile: docker/external/Dockerfile
    command: >
      sh -c "
      cd /app/planner &&
      gunicorn planner.wsgi:application 
      --bind 0.0.0.0:8000 
      --workers 10 
      --timeout 240 
      --access-logfile - 
      --error-logfile -
      "
    environment:
      - TZ=Europe/Moscow
      - DJANGO_SETTINGS_MODULE=planner.settings
      - PYTHONPATH=/app
      - BIND=0.0.0.0:8000
    volumes:
      - ../../:/app
    working_dir: /app
    expose:
      - "8000"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - web-network

  asgi:
    container_name: django_asgi
    build:
      context: ../..
      dockerfile: docker/external/Dockerfile
    command: >
      sh -c "
      cd /app/planner &&
      daphne -b 0.0.0.0 -p 8000 planner.asgi:application
      "
    environment:
      - TZ=Europe/Moscow
      - DJANGO_SETTINGS_MODULE=planner.settings
      - PYTHONPATH=/app
    volumes:
      - ../../:/app
    working_dir: /app
    expose:
      - "8000"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - web-network

  nginx:
    container_name: django_nginx
    image: nginx:alpine
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - ../../planner/static:/app/planner/static:ro    # Статика из корня
      - ../../planner/media:/app/planner/media:ro      # Медиа из корня
    ports:
      - "80:80"
      - "443:443"
    environment:
      - TZ=Europe/Moscow
    depends_on:
      - wsgi
      - asgi
    restart: unless-stopped
    networks:
      - web-network

networks:
  web-network:
    driver: bridge
